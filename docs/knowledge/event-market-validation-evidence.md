# Event-market validation evidence

This page gives stable acceptance (`AC-EM-*`) and evidence (`EV-EM-*`) IDs to
the one-to-one criteria in the protected
[event-market specification](../specs/event-markets.md#required-validation).
It is an evidence index, not a replacement for the normative specification.

Test names below are stable lookup keys for candidate-head automation. A pull
request that relies on this matrix must record its candidate commit, the exact
command or CI job URL, and the result for every cited `EV-EM-*` ID. A named
test is not evidence for a newer candidate until it has passed on that
candidate.

| Acceptance ID | Evidence ID | Observable criterion                                                                                                                                                                                                                                                     | Candidate-head automated evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Live evidence still required                                                                                                                                                          |
| ------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `AC-EM-01`    | `EV-EM-01`  | Organizer publication supports an empty collection, optional organizer pickup, external signing, and exact retry.                                                                                                                                                        | `tests/event-market-publish.test.ts`: `publishes an organizer event without forcing an organizer pickup`; `publishes calendar and pickup ACKs before publishing the collection`; `retries the exact signed record without rebuilding or resigning it`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Confirm one real NIP-07 session and one real NIP-46 session can sign the flow without exporting keys.                                                                                 |
| `AC-EM-02`    | `EV-EM-02`  | Accepted merchant-authored booth pickup stays merchant handoff and sends no organizer receipt.                                                                                                                                                                           | `tests/event-market-resolution.test.ts`: `derives merchant handoff from the product's exact direct booth pickup`; `tests/order-pickup-fulfillment.test.ts`: `accepts a merchant-authored booth pickup only as merchant handoff`; `e2e/event-market.playwright.ts`: `organizer offer off publishes an empty catalog and permits booth handoff @market @merchant`                                                                                                                                                                                                                                                                                                                                                                                                         | Run the browser flow against the candidate previews and observed relay set.                                                                                                           |
| `AC-EM-03`    | `EV-EM-03`  | Organizer handoff authority requires an authenticated merchant receipt bound to the exact current mutual graph and merchandise. A usable declared organizer inbox is required to deliver and discover receipts, not to prove global completeness after one is found.     | `tests/event-market-resolution.test.ts`: `requires one valid signed product price before acceptance or ACK`; `tests/event-market-handoff.test.ts`: `requires a current secure organizer kind-10050 declaration`; `tests/merchant-organizer-handoff-authorization.test.ts`: `uses Core-verified exact product titles and permits positive partial coverage`                                                                                                                                                                                                                                                                                                                                                                                                              | Confirm the organizer declaration and graph events propagate to the relays used by the candidate previews.                                                                            |
| `AC-EM-04`    | `EV-EM-04`  | No valid merchant authorization means no handoff authority. Invalid, ambiguous, stale, deleted, conflicting, unsupported, or cross-author positive evidence fails closed.                                                                                                | `tests/event-market-resolution.test.ts`: `fails closed on malformed, conflicting, and unsupported collection graphs`; `rejects attacker-owned calendars and never grants attacker pickup handoff`; `tests/event-market-handoff.test.ts`: `finds no handoff authority without a valid merchant receipt`; `binds ACK authority to current two-sided organizer handoff evidence`                                                                                                                                                                                                                                                                                                                                                                                           | None beyond normal candidate-head automation unless graph resolution changes.                                                                                                         |
| `AC-EM-05`    | `EV-EM-05`  | The normal Merchant UI requires explicit action-time confirmation that payment is settled or nothing is owed, the order is ready, and organizer release is authorized. The authenticated receipt asserts all three facts.                                                | `tests/merchant-event-market-handoff.test.ts`: `requires explicit merchant confirmation before creating a ready authorization`; `permits zero-cost readiness and blocks nonzero unpaid readiness`; `tests/event-market-handoff.test.ts`: `derives a minimal receipt from the private order and current graph`; `tests/merchant-event-market-route-contract.test.ts`: `keeps handoff consent explicit and organizer authority narrowly scoped`                                                                                                                                                                                                                                                                                                                           | Exercise one paid and one zero-cost order in candidate previews. Do not include invoices or payment secrets in artifacts.                                                             |
| `AC-EM-06`    | `EV-EM-06`  | Ready evidence contains only the literal authorization assertions, exact graph, quantities, claim, state, and time; it rejects contact, address, notes, invoices, proofs, secrets, and extra keys.                                                                       | `tests/merchant-event-market-handoff.test.ts`: `builds only the minimal receipt and excludes private order fields`; `tests/event-market-handoff.test.ts`: `round-trips exact authority and rejects sensitive or free-form fields`; `includes pickup lines only and fails closed on duplicate coordinates`                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Inspect only decoded schema field names with disposable identities; do not capture payload values in logs or video.                                                                   |
| `AC-EM-07`    | `EV-EM-07`  | Exact receipt, revocation, and ACK wraps persist before I/O; zero/partial delivery remains retryable and idempotent across reload. Completed receipt seeds do not exhaust durable storage.                                                                               | `tests/event-market-handoff.test.ts`: `persists exact signed wraps before relay I/O and reports partial ACKs`; `converges alternating relay ACKs without republishing exact wraps`; `tests/merchant-event-market-handoff.test.ts`: `reloads one durable receipt idempotently without republishing`; `releases completed ready receipts without filling delivery or marker storage`; `preserves retryable ready receipts and revocation rows on completion cleanup`; `does not clobber a concurrent revocation upsert when releasing a completed receipt`; `collects an interrupted pending completion marker on the next upsert`                                                                                                                                        | With disposable accounts, interrupt one candidate-preview delivery, reload, and confirm the exact pending delivery converges without a second logical receipt.                        |
| `AC-EM-08`    | `EV-EM-08`  | Bounded coverage remains visible as discovery health. Pagination continues to find relevant messages, but capped, partial, stale, or otherwise incomplete coverage does not negate a valid authorization or acknowledgement already found.                               | `tests/event-market-handoff.test.ts`: `authorizes a found valid receipt despite incomplete inbox coverage`; `authorizes a found receipt when an exact timestamp boundary stays capped`; `discovers paginated receipt evidence without certifying a multi-request scan`; `discovers evidence past 3,200 wraps but keeps stitched coverage partial`; `tests/merchant-event-market-handoff.test.ts`: `preserves a found exact ACK while reporting degraded discovery`; `e2e/event-market.playwright.ts`: `organizer handoff completes a private order receipt and exact ACK flow @market @merchant`                                                                                                                                                                        | Confirm degraded discovery remains visible while the found valid authorization stays actionable. A browser reload intentionally restarts the process-local scan from the newest page. |
| `AC-EM-09`    | `EV-EM-09`  | A valid matching revocation known before handoff removes readiness and prevents handout. The possible existence of an unseen revocation does not negate found authority. Organizer ACK grants no merchant-only lifecycle authority.                                      | `tests/event-market-handoff.test.ts`: `authorizes a found valid receipt despite incomplete inbox coverage`; `dedupes exact retries and treats ACK plus revocation as conflicting`; `tests/merchant-event-market-handoff.test.ts`: `allows revocation without completeness proof but never over a known acknowledgement`; `blocks cancellation when a scoped handed-out acknowledgement exists`; `tests/merchant-event-market-route-contract.test.ts`: `verifies the signed pickup snapshot before the Merchant order workflow`                                                                                                                                                                                                                                          | Confirm the candidate UI disables handout for a known revoked receipt and leaves ordinary completion authored by the merchant.                                                        |
| `AC-EM-10`    | `EV-EM-10`  | Signed-in pickup and a completed guest merchant-present sale need no contact form; guest pickup that continues later requires one merchant-only recovery method.                                                                                                         | `tests/buyer-checkout.test.ts`: `allows signed-in pickup without contact or an address`; `requires only one merchant recovery method for guest pickup`; `tests/order-pickup-fulfillment.test.ts`: `requires one merchant-only recovery method for guest pickup`; candidate-head merchant-present coverage must prove the completed guest path omits recovery contact.                                                                                                                                                                                                                                                                                                                                                                                                   | Verify signed-in, completed guest booth, and later guest pickup forms, and confirm the organizer view never displays guest recovery data.                                             |
| `AC-EM-11`    | `EV-EM-11`  | Cross-app browser coverage exercises creation, both handoff modes, acceptance, pickup checkout, readiness, organizer ACK, and merchant completion.                                                                                                                       | `e2e/event-market.playwright.ts`: `organizer offer off publishes an empty catalog and permits booth handoff @market @merchant`; `organizer handoff completes a private order receipt and exact ACK flow @market @merchant`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Run both fixtures on the candidate head. Record separate paid and zero-cost results if the fixtures do not cover both branches, plus real external-signer results from `EV-EM-01`.    |
| `AC-EM-12`    | `EV-EM-12`  | Ordinary shipping, digital, legacy order, and non-event collection behavior remains unchanged.                                                                                                                                                                           | `tests/buyer-checkout.test.ts`: `adds known physical shipping costs to checkout totals`; `keeps digital shipping ready while stale fiat pricing blocks direct payment`; `preserves item fulfillment format and defaults legacy items to physical`; `tests/order-pickup-fulfillment.test.ts`: `rejects mismatched product identity, delivery addresses, and mixed shipping`                                                                                                                                                                                                                                                                                                                                                                                              | Run the normal Market and Merchant E2E shards on the candidate; add a focused live regression only when those ordinary surfaces changed.                                              |
| `AC-EM-13`    | `EV-EM-13`  | Merchant/event configuration chooses exactly one arrangement before publication; organizer handoff is unavailable without a current usable organizer offer, and local intent grants no protocol authority.                                                               | `tests/merchant-event-handoff-arrangement.test.ts`: `stores the first choice by account and makes later products inherit it`; `refuses organizer handoff when the organizer has no current offer`; `keeps partial evidence distinct from an unconfigured complete read`; `tests/merchant-event-handoff-ui-contract.test.ts`: `configures one event arrangement and blocks unresolved publication`; `hides unavailable organizer handoff and reports transition progress`                                                                                                                                                                                                                                                                                                | In candidate previews, configure one event with and one without an organizer pickup and record only the visible choice/state, not identities or coordinates.                          |
| `AC-EM-14`    | `EV-EM-14`  | Event-led publishing, template copies, ordinary product create/edit, and organizer acceptance use one validator; new products inherit the established exact pickup and contradictory output cannot be signed.                                                            | `tests/merchant-event-product-publishing.test.ts`: `inherits the merchant/event arrangement for blank and copied drafts`; `tests/merchant-product-local-pickup.test.ts`: `guards unrelated edits until existing fulfillment is resolved explicitly`; `tests/merchant-event-product-acceptance.test.ts`: `rejects a contradictory pickup before organizer acceptance`; `enforces the invariant at the shared organizer membership boundary`; `accepts another product only when the merchant/event handoff matches`; `e2e/event-market.playwright.ts`: `organizer publishes and accepts their own product as merchant pickup @market @merchant`; `organizer offer off publishes an empty catalog and permits booth handoff @market @merchant`                            | With an external signer, publish through both entry points and verify the review UI shows the same inherited arrangement before each signature prompt.                                |
| `AC-EM-15`    | `EV-EM-15`  | Two products for one merchant/event reuse one pickup and complete one combined purchase; unrelated bag contents remain outside the isolated purchase group.                                                                                                              | `tests/merchant-event-handoff-arrangement.test.ts`: `uses one deterministic merchant pickup identity per merchant and event`; `tests/cart-model.test.ts`: `separates merchant-present candidates from remote pickup and preserves only compatible intent`; `e2e/event-market.playwright.ts`: `merchant-present guest and signed-in purchases stay isolated and merchant-authorized @market @merchant` composes two exact event products, one booth purchase, and one unrelated remote line.                                                                                                                                                                                                                                                                             | Repeat the combined purchase in desktop and mobile candidate previews with disposable merchandise and no captured payload values.                                                     |
| `AC-EM-16`    | `EV-EM-16`  | Equivalent legacy merchant pickups can be consolidated deliberately; conflicting legacy or external listings require explicit reconciliation and cannot be silently accepted or checked out.                                                                             | `tests/merchant-event-handoff-arrangement.test.ts`: `classifies equivalent legacy pickup records separately from conflicts`; `surfaces mixed listing modes and an in-progress transition`; `tests/merchant-event-product-acceptance.test.ts`: `keeps ambiguous handoff evidence unaccepted`; `rejects a contradictory pickup before organizer acceptance`; `tests/market-event-fulfillment.test.ts`: `blocks pickup lines from different organizer event graphs before checkout`; `blocks one merchant order when pickup handlers differ`                                                                                                                                                                                                                               | Import disposable legacy fixtures in the candidate UI and confirm the affected-listing review is explicit before any signature prompt.                                                |
| `AC-EM-17`    | `EV-EM-17`  | Arrangement changes enumerate affected listings, persist exact signed operations, obtain required organizer acceptance, expose per-record outcomes, and retry only exact unresolved events without overstating partial completion.                                       | `tests/merchant-event-handoff-change-runtime.test.ts`: `reads one exact current revision for every merchant event listing`; `refuses a partial product read before an arrangement-wide mutation`; `tests/merchant-event-handoff-change.test.ts`: `persists two exact signatures, reports a mid-batch failure, and retries without re-signing`; `stops before signing the next listing when an exact source revision drifts`; `keeps the journal until a fresh organizer collection accepts changed listings`; `tests/merchant-event-handoff-transition.test.ts`: `does not report unsigned or zero-ack listings as changed`                                                                                                                                             | In a disposable relay plan, force one ACK, one reject, and one timeout; capture only content-free per-record outcomes and verify the UI does not report full completion.              |
| `AC-EM-18`    | `EV-EM-18`  | Pre-transition orders retain their original payment/fulfillment snapshot and privacy boundary; continuation requires a durable checkpoint plus exact verified old graph and never adds organizer sharing to old merchant handoff.                                        | `tests/merchant-event-handoff-change-runtime.test.ts`: `checkpoints every affected existing order and leaves unrelated orders alone`; `blocks before mutation when the private order inbox is incomplete`; `tests/merchant-event-handoff-change.test.ts`: `requires existing-order checkpoint completion before any product signing`; `tests/merchant-order-pickup-authority-checkpoint.test.ts`: `never upgrades a merchant-only order after the current event changes`; `uses the exact signed snapshot for an existing order after live revisions change`; `rejects legacy omitted authority and mixed fulfillment`                                                                                                                                                  | Inspect redacted candidate UI states after a transition; do not use or record real order payloads.                                                                                    |
| `AC-EM-19`    | `EV-EM-19`  | Merchant-present authority is a current merchant-authenticated private authorization for the exact purchase group; URL/checkbox context, cached stock, expiry, replay, or mismatched revisions cannot override stock, price, destination, settlement, or handler checks. | `tests/merchant-present-sale-authorization.test.ts`: `binds the exact cart, merchant, total, and payment destination reviewed by the buyer`; `builds one minimal capability for exact graph and product revisions`; `rejects changed reviewed terms, quantities, and product revisions`; `rejects organizer handoff, legacy ambiguity, and forged rumor bindings`; `keeps the capability short-lived and physically scoped`; `requires an atomic single-use nonce consumption`; `tests/merchant-present-order-ui-contract.test.ts`: `rechecks the signed merchant payment destination before consuming booth confirmation`                                                                                                                                              | Use a real external signer to issue one authorization and inspect only its field names and verification result; keep contents out of artifacts.                                       |
| `AC-EM-20`    | `EV-EM-20`  | Signed-in and guest merchant-present flows review one total, pay the merchant, and complete only after merchant payment and handoff confirmation, without shipping UI, remote fields, or the generic availability warning.                                               | `tests/merchant-present-order-authorization.test.ts`: `uses the session-bound booth order when guest cache recovery is unavailable`; `uses the signed-in conversation when the local cache is unavailable`; `moves from waiting to ready using the exact signed-in conversation`; `tests/merchant-present-sale-merchant-delivery.test.ts`: `uses the private order channel for signed-in buyers only`; `prepares one exact direct wrap for a guest without publishing`; `e2e/event-market.playwright.ts`: `merchant-present guest and signed-in purchases stay isolated and merchant-authorized @market @merchant`. The guest tab retains only its exact session order and direct authorization; the merchant retains the authoritative encrypted lifecycle record.     | Run both identities at a mobile viewport and on a physical mobile browser. A real wallet settlement and signer prompt remain human QA unless actually observed.                       |
| `AC-EM-21`    | `EV-EM-21`  | Remote merchant pickup remains distinguishable from merchant-present sale and preserves merchant-owned payment confirmation, truthful preparation/readiness/completion states, and bounded merchant-only guest recovery.                                                 | `tests/merchant-present-order-authorization.test.ts`: `keeps an ordinary remote merchant-pickup order outside the booth gate`; `tests/merchant-present-sale-authorization.test.ts`: `does not upgrade a remote merchant-pickup order at authorization time`; `tests/order-pickup-fulfillment.test.ts`: `distinguishes merchant-present guest pickup from remote pickup`; `requires one merchant-only recovery method for guest pickup`; `tests/order-view.test.ts`: `shows every organizer-handoff readiness state, including guest recovery`; `e2e/event-market.playwright.ts`: `guest remote merchant pickup reaches a manual invoice without reading unselected pickup options @market`                                                                              | Verify remote entry never claims physical presence and that only the merchant view contains a disposable guest recovery field.                                                        |
| `AC-EM-22`    | `EV-EM-22`  | Organizer-assisted purchase composes merchant payment confirmation, preparation, minimal ready receipt, revocation/ack safeguards, organizer collection, and merchant completion without exposing or transferring merchant-only authority.                               | `tests/merchant-payment-release.test.ts`: `publishes buyer readiness only as a merchant-authored status`; `keeps payment confirmed when receipt creation or delivery fails`; `tests/event-market-handoff.test.ts`: `derives a minimal receipt from the private order and current graph`; `round-trips exact authority and rejects sensitive or free-form fields`; `dedupes exact retries and treats ACK plus revocation as conflicting`; `tests/order-status.test.ts`: `recognizes pickup readiness without treating it as payment proof`; `e2e/event-market.playwright.ts`: `organizer handoff completes a private order receipt and exact ACK flow @market @merchant`                                                                                                 | Complete one disposable candidate-preview handoff and inspect only state labels, pickup code parity, and content-free delivery outcomes.                                              |
| `AC-EM-23`    | `EV-EM-23`  | Two-tab cart persistence and purchase-group isolation remain correct for event checkout: no lost, duplicated, or unrelated lines and no stale tab can silently broaden the reviewed purchase.                                                                            | Inherited from the reviewed cart dependency: `e2e/market-cart-concurrency.playwright.ts`: `delayed stale quantity actions cannot restore a line removed in another tab @market`; `atomic quantity deltas preserve concurrent changes across signed-out tabs @market`; `atomic concurrent additions preserve both new lines across signed-out tabs @market`; `a stale remove consumes only observed quantities and preserves a concurrent add @market`; `a mid-session storage failure preserves exact purchase cleanup in memory @market`; `mixed shipping, two events, and two merchants become separate purchasable groups @market`. Candidate composition: `merchant-present guest and signed-in purchases stay isolated and merchant-authorized @market @merchant`. | Repeat the two-tab sequence in a real mobile browser when browser storage-event behavior differs from automated Chromium.                                                             |
| `AC-EM-24`    | `EV-EM-24`  | Exact-candidate desktop/mobile evidence covers both publishing entry points and merchant-present, remote merchant-pickup, and organizer-handoff journeys; unobserved device, signer, relay, and live-payment behavior remains an explicit human gate.                    | Record the exact candidate SHA and results for `organizer publishes and accepts their own product as merchant pickup @market @merchant`; `organizer offer off publishes an empty catalog and permits booth handoff @market @merchant`; `guest remote merchant pickup reaches a manual invoice without reading unselected pickup options @market`; `merchant-present guest and signed-in purchases stay isolated and merchant-authorized @market @merchant`; and `organizer handoff completes a private order receipt and exact ACK flow @market @merchant`, plus the focused unit/integration commands cited by `EV-EM-13` through `EV-EM-23`. Injected signers prove browser composition only.                                                                         | Physical mobile Safari/Chrome, real NIP-07 and NIP-46, intended-relay ACK/readback, and live Lightning payment confirmation require named human QA with outcome and timestamp.        |

## Evidence artifact rules

- Use disposable identities and redact account identifiers from screenshots,
  recordings, logs, and CI attachments.
- Never record order contents, contact details, addresses, ciphertext, invoices,
  signer secrets, wallet connection material, or message bodies.
- Record coarse outcomes only: event kind, state class, aggregate relay counts,
  and content-free ACK, reject, or timeout status.
- Treat injected Playwright signers as browser automation evidence, not proof of
  real third-party NIP-07 or NIP-46 prompt behavior.
- The bounded inbox continuation is process-local. It can discover older
  positive or revocation evidence across calls, but pagination and stitched
  calls remain partial. Coverage describes discovery and the uncertainty of
  absence; it is not an authority gate for exact authenticated evidence already
  found. Reloading safely discards parsed evidence and restarts from the newest
  relay page; it does not claim restart-durable deep-scan progress.
